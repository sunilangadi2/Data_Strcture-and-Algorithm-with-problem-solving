public class Solution {
    public static int predictAge(int ...ages) {
    
        int sum = 0;
        
        for(int age : ages){
            sum += age * age;
        }
        
        double root = Math.sqrt(sum);
        
        return (int)(root / 2);
    }
}